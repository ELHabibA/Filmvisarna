

export class RestApi {
    constructor(server, settings) {
        const {
            connection,
            restPrefix: prefix,
            debug
        } = settings;
        this.server = server;
        this.settings = settings;
        this.prefix = prefix;
        this.debug = debug;
        Qu
    }
}